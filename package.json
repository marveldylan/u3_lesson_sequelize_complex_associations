{
  "name": "sequelize-complex-associations",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "setup": "npm install && npx sequelize db:create && npx sequelize db:migrate && npx sequelize db:seed:all"
  }
}
